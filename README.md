# NotebookLM Watermark Remover

这是一个基于 Streamlit 开发的 Python Web 工具，旨在帮助用户快速、批量地移除图片右下角固定的 "NotebookLM" 水印。

## ✨ 功能特点

- **可视化调整**：通过红色半透明遮罩框，实时调整需要覆盖的水印区域。
- **批量预览**：支持一次性上传多张图片，并对比原图与去水印后的效果。
- **智能取色**：支持从遮罩区域左侧或上方自动吸取背景色，确保覆盖自然。
- **macOS 友好**：支持通过 Finder 原生选择文件夹路径来保存处理后的图片。
- **一键处理**：满意预览效果后，点击即可批量保存到指定本地目录。

## 🚀 快速开始

### 1. 克隆/进入项目目录
确保您已进入项目文件夹。

### 2. 启动应用
项目提供了一个便捷的启动脚本，会自动创建虚拟环境并安装必要的依赖：

```bash
./start.sh
```

### 3. 使用步骤
1.  **上传图片**：点击界面顶部的上传区域，选择一张或多张图片。
2.  **调整遮罩**：
    - 在左侧侧边栏调节 `Mask Width` 和 `Mask Height`。
    - 观察预览图中**左侧图片上的红色方块**，调整至刚好盖住水印。
3.  **选择路径**：
    - 点击 "Browse..." 按钮选择图片保存的位置（仅限 macOS）。
    - *首次点击时，请在系统弹窗中允许“终端”控制“System Events”。*
4.  **保存结果**：点击 "Save All Processed Images" 按钮。

## 🛠️ 技术栈

- **Streamlit**: Web UI 框架。
- **Pillow (PIL)**: 图像处理核心库。
- **AppleScript**: 用于实现 macOS 原生文件夹选择器。

## ⚠️ 注意事项 (macOS)

为了让文件夹选择器（Browse 按钮）正常工作，您可能需要：
1.  当系统询问是否允许控制 **System Events** 时，选择“允许”。
2.  如果点击无反应，请检查：`系统设置` -> `隐私与安全性` -> `自动化` -> 确保您的终端应用（如 Terminal 或 iTerm2）下的“System Events”已开启。

## 📄 开源协议
MIT